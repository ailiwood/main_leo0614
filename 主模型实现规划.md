# 主模型实现规划.md

> 项目根路径（固定）：`E:\00project_code\main_leo\new_code`  
> 本文件是主模型实现的最高技术依据。  
> 核心修正：DEConv、Data2Vec-Audio、CME 不是默认关闭后忽略，而是候选增强模块，必须通过真实实验裁决是否进入最终主模型。  
> 旧主模型架构图作废，不得参考；最终架构图必须根据最终冻结代码重新绘制。

---

## 1. 主模型研究问题

本论文拟解决多模态情感分析中的三个问题：

1. 单模态长程时序依赖建模不足；
2. 多模态贡献随样本动态变化，但传统融合方法常使用静态或难解释权重；
3. 视觉动态、音频高层语义和跨模态交互是否能增强最终性能，需要真实实验裁决。

因此，本轮主模型不是固定成单一静态结构，而是采用：

```text
必须实现主干 + 候选增强模块池 + 数据实验裁决 + 最终主模型冻结
```

---

## 2. 最低必须实现主干

最低主干用于保证论文题目、核心创新与代码一致：

```text
输入：text / audio / vision 三模态序列特征
  ↓
统一投影：Linear(d_m → d) + LayerNorm + Dropout
  ↓
三路 sLSTM 时序编码：H_t, H_a, H_v
  ↓
Masked pooling 得到 h_t, h_a, h_v
  ↓
AWAF 样本级自适应加权注意力融合
  ↓
多任务预测头：回归 + 分类 + 单模态辅助头
```

该最低主干不是最终模型的必然终点，而是所有候选模块实验的共同基线。

---

## 3. sLSTM 时序编码模块

### 3.1 实现原则

默认采用纯 PyTorch 自实现 sLSTM，避免官方库 triton / CUDA kernel 在 Windows + conda 环境下带来的不确定性。实现前必须审计仓库历史 `utils_models/lstm_v.py`、`vision_xLSTM` 等文件：

1. 判断其实现 sLSTM 还是 mLSTM；
2. 判断是否包含指数门、normalizer、stabilizer；
3. 判断许可证与来源是否清楚；
4. 判断是否可 wrapper 复用；
5. 若不清楚或不匹配，直接重写。

### 3.2 接口要求

```python
class SLSTMCell(nn.Module):
    """sLSTM 单元。输入 x_t: [B, D]，输出 h_t: [B, H]。"""

class SLSTMEncoder(nn.Module):
    """三模态时序编码器。输入 x: [B, T, D]，mask: [B, T]，输出 H: [B, T, H]。"""
```

关键代码必须使用中文注释，说明门控、normalizer、stabilizer、防 NaN 处理、mask 更新逻辑。

---

## 4. AWAF：样本级自适应加权注意力融合

AWAF 是本论文核心创新模块，必须独立实现、独立命名、独立消融、可保存权重、可画图解释。

### 4.1 输入输出

输入：

```text
h_t, h_a, h_v ∈ R^d
```

输出：

```text
Z ∈ R^d
w = [w_t, w_a, w_v]，每个样本一组三模态权重，sum(w)=1
```

### 4.2 两段式权重生成

第一段：跨模态上下文增强。

```text
对每个模态 h_m，以 h_m 为 query，以其他模态为 key/value，得到上下文向量 c_m。
增强表示：
  \hat{h}_m = LayerNorm(h_m + c_m)
```

第二段：二阶交互打分。

```text
g_ta = \hat{h}_t ⊙ \hat{h}_a
g_tv = \hat{h}_t ⊙ \hat{h}_v
g_av = \hat{h}_a ⊙ \hat{h}_v

e = MLP(concat[\hat{h}_t, \hat{h}_a, \hat{h}_v, g_ta, g_tv, g_av]) ∈ R^3
w = softmax(e / τ)
Z = w_t \hat{h}_t + w_a \hat{h}_a + w_v \hat{h}_v
```

`τ` 为可学习温度，初始 1.0。训练时可启用 `modality_dropout`，随机置零部分模态摘要，增强鲁棒性。

### 4.3 AWAF 消融开关

| 开关 | 含义 |
|---|---|
| `awaf` | 完整 AWAF |
| `awaf_no_interaction` | 去掉二阶交互项 |
| `awaf_no_context` | 去掉跨模态上下文增强 |
| `mean` | 等权平均 |
| `gated` | 普通门控融合 |
| `concat` | 拼接 + MLP |
| `fixed` | 全局固定可学习权重 |
| `modality_dropout_off` | 关闭模态 dropout |

---

## 5. 候选增强模块池

### 5.1 DEConv

定位：视觉动态增强候选模块。用于强化视觉局部动态变化、帧间差异、动作变化。不得在实验前写成已验证创新。

挂载位置：

```text
vision feature → DEConv → vision projection → vision sLSTM
```

### 5.2 Data2Vec-Audio

定位：音频特征增强/替换候选模块。用于验证更高层语音表征是否提升情感建模。

两种策略：

1. 使用已有音频特征作为基线；
2. 使用 Data2Vec-Audio 提取或复用特征作为候选增强。

### 5.3 CME

定位：跨模态交互候选模块。用于验证在 AWAF 前增加显式跨模态交互是否提升性能。

挂载位置候选：

```text
方案 A：sLSTM 输出 H_t/H_a/H_v → CME → pooling → AWAF
方案 B：projection 后 → CME → sLSTM → AWAF
```

优先测试方案 A，因为它不破坏最低主干的数据流，便于比较。

---

## 6. 候选模块裁决实验

### 6.1 实验矩阵

| 编号 | 结构 | 是否必须执行 |
|---|---|---|
| C0 | sLSTM + AWAF | 必须 |
| C1 | C0 + DEConv | 必须 |
| C2 | C0 + Data2Vec-Audio | 必须，若特征提取成本可控 |
| C3 | C0 + CME | 必须 |
| C4 | C0 + DEConv + CME | 建议 |
| C5 | C0 + Data2Vec-Audio + CME | 建议 |
| C6 | C0 + DEConv + Data2Vec-Audio | 资源允许执行 |
| C7 | C0 + DEConv + Data2Vec-Audio + CME | 资源允许执行 |

若显存或时间不足，必须在 handoff 中说明跳过原因，不得简单省略。

### 6.2 裁决标准

候选模块纳入最终主模型必须满足：

1. ACC2_Non0 或 F1_Non0 在验证集稳定提升；
2. MAE / Corr 不明显退化；
3. 至少 2 个 seed 方向一致；
4. 参数量、训练时间、显存成本可接受；
5. 能形成明确论文解释。

### 6.3 输出

```text
reports/P3_candidate_module_selection.md
reports/P3_candidate_results.xlsx
reports/P3_candidate_results.docx
outputs/P3_candidate_selection/<dataset>/<config>/<timestamp>_<seed>/
```

表格必须详细，包括所有指标、seed、耗时、参数量、是否纳入最终模型、裁决理由。

---

## 7. 最终主模型冻结

候选模块实验完成后，生成：

```text
docs/FINAL_MODEL_SPEC.md
configs/models/ours_final.yaml
reports/P4_final_model_freeze.md
```

`FINAL_MODEL_SPEC.md` 必须包含：

1. 最终结构图的文字版；
2. 纳入模块列表；
3. 未纳入模块列表；
4. 每个模块纳入/剔除的实验依据；
5. 对应代码文件路径；
6. 后续正式消融实验清单。

冻结后不得随意改变主模型，除非用户和网页版 AI 根据新证据共同确认。

---

## 8. 正式消融实验逻辑

正式消融只围绕最终冻结主模型进行。最低包括：

1. w/o AWAF；
2. AWAF → mean / gated / concat / fixed；
3. w/o AWAF 二阶交互；
4. w/o AWAF 跨模态上下文；
5. sLSTM → LSTM / GRU；
6. 去除最终纳入的 DEConv/Data2Vec-Audio/CME 中每一个模块；
7. 若某候选模块未纳入最终主模型，其结果作为“候选模块选择实验”，不强行放入最终消融主表。

---

## 9. 训练策略

正式训练必须满足：

1. epoch ≥ 50；
2. warmup + cosine annealing；
3. AdamW；
4. weight decay；
5. gradient clipping；
6. early stopping；
7. 多 seed；
8. 自动保存 best / last 权重；
9. 自动保存 predictions 与 AWAF weights；
10. 统一 `experiment_registry.csv` 登记。

---

## 10. 论文可解释性输出

必须输出 ≥900 dpi 图像：

1. AWAF 三模态权重分布；
2. 不同情感强度区间的权重变化；
3. 弱负样本、近零样本、模态冲突样本权重；
4. 候选模块选择对比图；
5. 最终消融柱状图；
6. 全模型 loss 曲线对比。

表格必须输出 xlsx 与 docx，详细到 seed 与 run 级别，不能只给总表。
