# 实验设计.md

> 项目根路径（固定）：`E:\00project_code\main_leo\new_code`  
> 本文件规定论文实验代码的最终设计、baseline 设定、环境设定、实验步骤计划和代码规范。  
> 本文件必须被 `claude.md`、`workflow.md`、`CC启动提示词.md` 引用并作为 CC/Codex 执行实验的直接依据。  
> 核心修正：DEConv、Data2Vec-Audio、CME 是候选增强模块，需通过真实数据实验裁决是否进入最终主模型，不是默认关闭后忽略。

---

## 1. 实验目标

本实验体系服务于硕士论文《基于扩展 LSTM 与 Transformer 的跨模态情感分析研究》。实验目标不是单纯跑出一个最高分，而是形成可复现、可比较、可消融、可解释的完整证据链，用于支撑论文第 3、4、5 章。

实验需回答：

1. sLSTM 是否比普通 LSTM/GRU 更适合当前三模态时序建模；
2. AWAF 是否比 mean、concat、gated、fixed 等融合方式更有效；
3. AWAF 的二阶交互项、跨模态上下文、modality dropout 是否有效；
4. DEConv、Data2Vec-Audio、CME 是否应进入最终主模型；
5. 最终主模型是否优于经典 baseline 与两个 2025 baseline；
6. 模态权重是否具有样本级变化和可解释性。

---

## 2. 数据集与任务

### 2.1 数据集

主数据集：

```text
CMU-MOSI
CMU-MOSEI
```

如后续 baseline 需要，可记录 UR-FUNNY、CH-SIMS 等，但论文主任务以 MOSI/MOSEI 为准。

### 2.2 任务

1. 连续情感强度回归：输出回归值；
2. 二分类：ACC2_Non0 / F1_Non0 为主列；
3. 含 0 二分类：ACC2_Has0 / F1_Has0 单独列；
4. 七分类：ACC7；
5. 回归指标：MAE / Corr。

---

## 3. 数据与特征设计

P1 先裁决特征方案，不直接默认自建或默认复用。

### 3.1 方案 A：复用 MLCL 标准特征

优点：

1. 可与 MLCL baseline 保持特征口径一致；
2. 节省特征提取时间；
3. 便于复现和横向比较。

要求：

1. 记录下载来源；
2. 记录特征维度、split、样本数、序列长度；
3. 验证标签与 MOSI/MOSEI 官方划分一致；
4. 写入 `data/README_data.md`。

### 3.2 方案 B：自建特征提取

若复用特征不适配，则自建特征：

1. 文本：BERT/RoBERTa 或与 baseline 对齐的文本特征；
2. 音频：COVAREP / wav2vec / Data2Vec-Audio 候选；
3. 视觉：FACET / OpenFace / 已有视觉特征，DEConv 作为候选增强。

自建特征必须固化版本号，保存提取脚本、日志、维度统计、失败样本列表。

---

## 4. 主模型实验组设计

### 4.1 最低主干模型 C0

```text
C0 = 三模态投影 + 三路 sLSTM + AWAF（二阶交互） + 多任务预测头
```

C0 是候选模块裁决实验的基准，不等于最终主模型一定只包含这些模块。

### 4.2 候选增强模块

| 模块 | 作用 | 是否预先纳入最终模型 |
|---|---|---|
| DEConv | 视觉动态增强 | 否，实验裁决 |
| Data2Vec-Audio | 音频高层表征增强 | 否，实验裁决 |
| CME | 跨模态交互增强 | 否，实验裁决 |

### 4.3 候选模块裁决实验矩阵

| 实验编号 | 模型结构 | 目的 | 优先级 |
|---|---|---|---|
| C0 | sLSTM + AWAF | 最低主干 | 必做 |
| C1 | C0 + DEConv | 判断视觉增强价值 | 必做 |
| C2 | C0 + Data2Vec-Audio | 判断音频增强价值 | 必做或说明不可做原因 |
| C3 | C0 + CME | 判断跨模态交互价值 | 必做 |
| C4 | C0 + DEConv + CME | 判断视觉增强与跨模态交互协同 | 建议 |
| C5 | C0 + Data2Vec-Audio + CME | 判断音频增强与跨模态交互协同 | 建议 |
| C6 | C0 + DEConv + Data2Vec-Audio | 判断特征双增强 | 资源允许 |
| C7 | C0 + DEConv + Data2Vec-Audio + CME | 全增强组合 | 资源允许 |

### 4.4 最终模型冻结规则

最终模型由 P3/P4 的实验结果决定。纳入模块须满足：

1. 主指标有稳定提升；
2. 非主指标无明显退化；
3. 多 seed 趋势一致；
4. 成本可接受；
5. 可在论文中解释。

冻结后生成 `docs/FINAL_MODEL_SPEC.md`。正式消融实验围绕该冻结模型展开。

---

## 5. Baseline 设计

### 5.1 经典 baseline

根据代码可获得性和资源，按优先级整理：

1. TFN；
2. LMF；
3. MulT；
4. MISA；
5. Self-MM；
6. MMIM；
7. MAG-BERT / MAG-XLNet；
8. UniMSE。

原则：经典 baseline 不改核心架构，只做路径适配、数据接口适配、指标复算和日志规范化。

### 5.2 2025 baseline：MLCL

采用：Zhuang et al. (2025), *IEEE Transactions on Multimedia*, `YetZzzzzz/MLCL`。

要求：

1. P0 核对本地是否已有 MLCL 代码；
2. P1 评估是否复用 MLCL 标准特征；
3. 单独记录 MLCL 环境要求；
4. 用统一 `metrics.py` 复算指标；
5. 不得把同名缩写的其他 MLCL/MCL 论文混入。

### 5.3 2025 baseline：CASP

CASP 是 test-time adaptation 方法，不是普通端到端 baseline。实验表格中必须单独标注：

```text
CASP = TTA baseline / adapted model
```

要求：

1. 保留 CASP 的 pretrain / contrastive adaptation / pseudo-label 阶段记录；
2. 记录 backbone；
3. 用统一指标复算；
4. 与普通 baseline 同表时加脚注，不得偷换概念。

---

## 6. 实验环境设计

### 6.1 推荐新建环境

推荐在以下路径新建：

```text
E:\Anaconda3\envs\mme_xlstm
```

原因：旧 `mme` 环境经历多轮实验，依赖可能混乱；MLCL、CASP、主模型依赖版本可能冲突。新建环境更利于复现。

### 6.2 P0 环境审计

P0 必须输出：

```text
Python 版本
Conda 环境列表
CUDA 版本
显卡型号与显存
PyTorch 版本
transformers 版本
是否可用 GPU
建议新建环境 yaml
```

### 6.3 环境文件

最终应生成：

```text
env/environment_mme_xlstm.yml
env/requirements_main.txt
env/requirements_mlcl.txt
env/requirements_casp.txt
```

若不同 baseline 必须使用不同环境，应明确记录，不强行塞进一个环境。

---

## 7. 实验步骤计划

### 7.1 P0：扫描与重建 handoff

输出 `HANDOFF_PHASE_00.md`，不依赖旧 handoff。

### 7.2 P1：数据与特征裁决

输出 `P1_feature_decision.md`，决定复用或自建特征。

### 7.3 P2：最低主干 smoke test

训练 1 epoch，验证能完整跑通。

### 7.4 P3：候选模块快速裁决

使用小数据或验证集快速实验，筛选最终模型结构。

### 7.5 P4：冻结最终模型

输出 `FINAL_MODEL_SPEC.md`。

### 7.6 P5：全量正式训练

MOSI/MOSEI，epoch≥50，多 seed，保存详细结果。

### 7.7 P6：baseline 接入

逐个接入，逐个验证，不能一次性堆叠混乱。

### 7.8 P7：正式消融

围绕最终冻结模型展开。

### 7.9 P8：论文材料输出

输出详细表格和 ≥900 dpi 图。

---

## 8. 训练入口与评估入口

最终必须提供：

```text
scripts/train_main.py
scripts/eval_main.py
```

### 8.1 train_main.py

要求：

1. 默认先训练最终主模型，再训练 baseline；
2. 支持 `--model` 单模型训练；
3. 支持 `--dataset`；
4. 支持 `--config`；
5. 支持 `--seed`；
6. 自动保存 run 产物；
7. 自动追加 `reports/experiment_registry.csv`。

### 8.2 eval_main.py

要求：

1. 一次性评估全部模型；
2. 统一指标口径；
3. 输出 xlsx + docx 三线表；
4. 输出 ≥900 dpi 图像；
5. 输出 predictions 复算结果；
6. 输出 AWAF 权重可解释性图。

---

## 9. 产物落盘规范

每个 run 固定保存：

```text
outputs/<phase>/<dataset>/<model>/<YYYYMMDD_HHMMSS>_<seed>/
├── config.yaml
├── command.txt
├── train.log
├── metrics_epoch.csv
├── metrics_best.json
├── best_model.pth
├── last_model.pth
├── loss_curve.png
├── predictions_test.csv
└── awaf_weights_test.csv（仅主模型或含 AWAF 模型）
```

全局登记表：

```text
reports/experiment_registry.csv
```

字段必须包括：时间戳、阶段、模型、数据集、seed、采样比例、特征版本、lr、batch size、epoch、best epoch、全部指标、可信度、是否入论文、config 路径、日志路径、权重路径。

---

## 10. 图像与表格要求

### 10.1 图像

所有论文候选图必须 ≥900 dpi，坐标轴、图例、标题、注释清楚。禁止终端截图。

### 10.2 表格

表格必须详细，输出 xlsx 与 docx。至少包括：

1. 主模型候选模块裁决表；
2. 主模型全量训练多 seed 表；
3. baseline 对比表；
4. 最终消融表；
5. AWAF 权重统计表；
6. 错误样本/弱负样本分析表。

---

## 11. 代码规范

1. 关键代码使用中文注释，术语可以英文；
2. 遵循 PEP8；
3. 使用 type hints；
4. 核心类和函数必须有 docstring；
5. 配置集中到 `configs/`；
6. 禁止散落硬编码路径；
7. 模型与 trainer/evaluator 解耦；
8. 所有模型经 registry 注册；
9. 指标唯一实现；
10. 必要时直接重写旧代码，不为旧代码妥协。


---

## 12. 外部资源获取与环境安装策略

实验过程中允许 CC/Codex 在必要时联网查询和下载公开资源，包括 GitHub baseline 代码、官方模型实现、作者提供的特征下载脚本、缺失 Python 包和官方依赖说明。执行边界如下：

1. **先判断必要性**：只有当本地缺少模块、baseline、数据特征或依赖无法满足实验要求时才下载。
2. **优先官方来源**：论文作者 GitHub、官方文档、IEEE/ACL/AAAI 页面、HuggingFace 官方模型页优先。
3. **下载后登记**：写入 `docs/EXTERNAL_SOURCES.md`，包括 URL、commit、许可证、用途、是否修改。
4. **环境隔离**：主模型优先使用 `mme_xlstm`；MLCL/CASP 若依赖冲突，允许独立环境；所有环境必须导出 yaml 与 pip freeze。
5. **不污染旧环境**：旧 `mme` 只作为对照或备份，不建议继续向其中安装大量新包。
6. **复现实验记录**：每个 baseline 的运行环境、依赖版本和命令必须写入对应报告及表格脚注。

---

## 13. 清理与归档策略

实验设计阶段不允许自动删除文件夹。允许 CC/Codex 自动识别无用文件夹和中间产物，但只允许生成清理建议：

```text
reports/cleanup_candidates.md
reports/final_cleanup_plan.md
```

正式清理只在 P10 且用户明确确认后执行。清理优先采取归档移动，而不是直接删除。归档位置建议：

```text
archives/cleanup_<YYYYMMDD_HHMMSS>/
backups/cleanup_<YYYYMMDD_HHMMSS>/
```

以下文件默认不得清理：正式实验日志、最佳模型权重、per-sample predictions、AWAF 权重、三线表、≥900 dpi 图像、`experiment_registry.csv`、最终配置文件、最终 handoff。

