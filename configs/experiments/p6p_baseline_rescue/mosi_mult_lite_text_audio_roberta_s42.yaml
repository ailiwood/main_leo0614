phase: P6P
model:
  model_name: mult_lite
  modality_mode: text_audio
  hidden_dim: 128
  dropout: 0.2
  use_pretrained_text: true
  text_input_dim: 1024
  rank: 8
  fusion_dim: 128
data:
  dataset: mosi
training:
  seed: 42
  epochs: 50
  min_epochs: 30
  early_stopping_patience: 10
  batch_size: 8
  lr: 0.0003
  weight_decay: 0.01
  test_final_once: true
output:
  root: outputs/P6P/baseline_rescue/mosi
