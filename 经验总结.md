# 经验总结.md：多模态情感分析项目经验总结

> 项目根路径（固定）：`E:\00project_code\main_leo\new_code`  
> 本文件记录可复用经验、避坑规则和流程规范。

---

## 1. 总原则

### 1.1 模型设计必须由实验裁决

DEConv、Data2Vec-Audio、CME 等模块不能提前写成“已验证创新”，也不能简单默认关闭后忽略。正确做法是：先作为候选增强模块实现，再通过真实数据实验裁决是否进入最终主模型。

### 1.2 旧架构图作废

旧主模型架构图不再代表本轮真实模型。代码重构、论文写作和最终绘图均不得参考该图。最终图必须由冻结后的代码结构反推。

### 1.3 必要时直接重写代码

旧代码不可运行、结构混乱、硬编码严重或与新设计不匹配时，直接重写比打补丁更符合第一性原理。重写必须记录原因、路径和验证结果。

### 1.4 先生成新 handoff

旧 handoff 已删除，防止干扰。本轮必须通过全量扫描重新生成 `HANDOFF_PHASE_00.md`。

---

## 2. 指标与结果经验

1. ACC2_Non0 / F1_Non0 是主列；
2. ACC2_Has0 / F1_Has0 必须单独列；
3. 所有模型走统一 `utils/metrics.py`；
4. 每个 run 登记到 `reports/experiment_registry.csv`；
5. 历史结果没有完整证据链，只能参考，不能入主表。

---

## 3. 代码经验

1. 关键代码必须有中文注释，术语可用英文；
2. 代码遵循 Python / PyTorch 常规工程规范；
3. 核心张量形状必须注释；
4. 不散落硬编码路径；
5. 不为复用旧代码牺牲新模型设计；
6. 先 smoke test，再全量训练。

---

## 4. 表图经验

1. 图像必须 ≥900 dpi；
2. 禁止终端截图；
3. 表格必须详细到 seed / run / 特征版本 / 日志路径；
4. xlsx 和 docx 两种格式都要输出；
5. AWAF 权重必须落盘并可视化。


---

## 8. 外部资源与环境经验

允许使用外部 GitHub 代码和新增 Python 包，但必须做到来源可追踪、许可证可解释、环境可复现。新依赖优先安装到新建环境或 baseline 专属环境，不污染旧环境。

## 9. 清理经验

“自动清理无用文件夹”风险很高，容易误删实验日志、权重或论文证据链。正确做法是自动扫描、自动生成清理建议，用户确认后再归档或删除。

