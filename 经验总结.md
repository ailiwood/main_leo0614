# 经验总结.md：多模态情感分析项目经验总结

> 项目根路径（固定）：`E:\00project_code\main_leo\new_code`  
> 本文件记录可复用经验、避坑规则和流程规范。

---

## 1. 总原则

### 1.1 模型设计必须由实验裁决

DEConv、Data2Vec-Audio、CME 等模块不能提前写成“已验证创新”，也不能简单默认关闭后忽略。正确做法是：先作为候选增强模块实现，再通过真实数据实验裁决是否进入最终主模型。

### 1.2 旧架构图作废

旧主模型架构图不再代表本轮真实模型。代码重构、论文写作和最终绘图均不得参考该图。最终图必须由冻结后的代码结构反推。

### 1.3 必要时直接重写代码

旧代码不可运行、结构混乱、硬编码严重或与新设计不匹配时，直接重写比打补丁更符合第一性原理。重写必须记录原因、路径和验证结果。

### 1.4 先生成新 handoff

旧 handoff 已删除，防止干扰。本轮必须通过全量扫描重新生成 `HANDOFF_PHASE_00.md`。

---

## 2. 指标与结果经验

1. ACC2_Non0 / F1_Non0 是主列；
2. ACC2_Has0 / F1_Has0 必须单独列；
3. 所有模型走统一 `utils/metrics.py`；
4. 每个 run 登记到 `reports/experiment_registry.csv`；
5. 历史结果没有完整证据链，只能参考，不能入主表。

---

## 3. 代码经验

1. 关键代码必须有中文注释，术语可用英文；
2. 代码遵循 Python / PyTorch 常规工程规范；
3. 核心张量形状必须注释；
4. 不散落硬编码路径；
5. 不为复用旧代码牺牲新模型设计；
6. 先 smoke test，再全量训练。

---

## 4. 表图经验

1. 图像必须 ≥900 dpi；
2. 禁止终端截图；
3. 表格必须详细到 seed / run / 特征版本 / 日志路径；
4. xlsx 和 docx 两种格式都要输出；
5. AWAF 权重必须落盘并可视化。


---

## 8. 外部资源与环境经验

允许使用外部 GitHub 代码和新增 Python 包，但必须做到来源可追踪、许可证可解释、环境可复现。新依赖优先安装到新建环境或 baseline 专属环境，不污染旧环境。

## 5. P5C 关键经验：预训练特征不应重复建模

DeBERTa-large token features 已经通过 self-attention 完成了上下文化，再叠加 sLSTM 会造成语义退化（76.2% → 80.2% by removing sLSTM）。预训练 Transformer 输出不需要额外 RNN；sLSTM/xLSTM 应专注于 audio/vision 等需要时序建模的非文本模态。

## 6. P5C 关键经验：多模态融合应是残差修正

当单模态 text 已经达到 80.2% 而多模态只有 78.8% 时，多模态分支不应是”主融合器”而应是”残差修正器”。Final = text_base + λ * residual_correction 比三路平权融合更合理。

## 7. P5G/P6A/P6B 关键经验：大模型 frozen 特征在小数据集上无效

wav2vec2-large (1024d) 和 CLIP ViT-L/14 (1024d) 的 frozen 特征在 MOSI (1284训练样本) 上均比 base 模型 (768d) 差：
- Audio large: 80.18% vs 82.47% baseline (-2.29%)
- Vision large: 79.12% vs 82.47% baseline (-3.35%)
- AWAF 权重证据：模型主动将 audio 权重从 0.15 降至 0.03

**结论**：小数据集上，768d base 模型特征优于 1024d large 模型。不要盲目升级特征维度。

## 8. P6C 关键经验：Fine-tuned 文本骨干远超 frozen 特征

RoBERTa-large fine-tuned text-only (85.37%) 比 frozen DeBERTa text-only (80.2%) 提升 +5.17%。
在小数据集上，fine-tune 预训练模型比使用 frozen 特征有效得多。
这是本项目最重要的突破性发现。

## 9. 清理经验

“自动清理无用文件夹”风险很高，容易误删实验日志、权重或论文证据链。正确做法是自动扫描、自动生成清理建议，用户确认后再归档或删除。



---
## P6M Baseline-Lite 路线 (2026-06-19)
Baseline 路线固定为内部 baseline-lite 轻量复现。外部仓库仅作结构参考/引文来源/原论文报告值来源。
固定 baseline: TFN-lite, LMF-lite, MulT-lite, MISA-lite, SelfMM-lite, MMIM-lite, MLCL-lite, DLF-lite。
第一批: TFN-lite, LMF-lite, MulT-lite, SelfMM-lite。
CASP 不入普通baseline主列(TTA)。DPDF-LQ/DashFusion/R3DG暂不执行。
原论文报告值标注 'Reported by original paper'。Baseline-lite结果必须来自真实训练+utils/metrics.py统一复算。
教学演示/模拟/占位不得进入论文正式实验表。
