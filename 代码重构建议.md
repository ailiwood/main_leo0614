# 代码重构建议.md

> 项目根路径（固定）：`E:\00project_code\main_leo\new_code`  
> 只读参考路径：`E:\00project_code\main_leo\mme\Tri_modal_ER`、`E:\00project_code\main_leo\refer`  
> 本文件规定代码结构、注释、保存、复现、表图输出和重写规则。  
> 旧主模型架构图作废，重构过程不得参考该图。

---

## 1. 重构原则

结构服务于五个目标：

```text
可运行、可复现、可比较、可消融、可解释
```

不得为了保留旧文件结构而牺牲新模型设计。若旧代码与本轮实验设计冲突，应直接重写。

---

## 2. 目标目录结构

```text
new_code/
├── claude.md
├── workflow.md
├── 实验设计.md
├── 主模型实现规划.md
├── 代码重构建议.md
├── 最新学术进展与代码推荐.md
├── memory.md
├── 经验总结.md
├── configs/
│   ├── default.yaml
│   ├── data.yaml
│   ├── models/
│   │   ├── ours_backbone.yaml
│   │   ├── ours_candidate_deconv.yaml
│   │   ├── ours_candidate_data2vec.yaml
│   │   ├── ours_candidate_cme.yaml
│   │   ├── ours_final.yaml
│   │   ├── mlcl.yaml
│   │   └── casp.yaml
│   └── experiments/
├── data/
│   ├── dataset.py
│   ├── dataloader.py
│   ├── feature_extraction/
│   └── README_data.md
├── models/
│   ├── base.py
│   ├── encoders/
│   │   ├── slstm.py
│   │   ├── lstm_gru.py
│   │   ├── deconv.py
│   │   └── data2vec_audio.py
│   ├── fusion/
│   │   ├── awaf.py
│   │   ├── cme.py
│   │   └── fusion_baselines.py
│   ├── heads.py
│   ├── ours_xlstm_fusion.py
│   └── baselines/
├── engine/
│   ├── trainer.py
│   ├── evaluator.py
│   └── registry.py
├── utils/
│   ├── metrics.py
│   ├── seed.py
│   ├── logger.py
│   ├── io.py
│   ├── plot.py
│   └── tables.py
├── scripts/
│   ├── train_main.py
│   ├── eval_main.py
│   ├── tune_ours.py
│   └── extract_features.py
├── reports/
├── outputs/
├── external/                      # 外部公开代码原样保存，需登记来源和许可证
├── third_party/                   # 经适配的小型第三方模块
├── archives/                      # 用户确认后的清理归档
├── docs/
│   ├── FILE_STRUCTURE.md
│   ├── FINAL_MODEL_SPEC.md
│   ├── HYPERPARAMS.md
│   └── DECISIONS.md
└── env/
```

---

## 3. 必要时直接重写代码

以下情况必须直接重写，而不是继续参考旧代码：

1. 旧代码入口不可运行；
2. 旧代码数据流与最终主模型冲突；
3. 旧代码无法支持候选模块裁决实验；
4. 旧代码无法支持 `train_main.py` / `eval_main.py`；
5. 旧代码指标口径不统一；
6. 旧代码注释缺失、命名混乱、硬编码严重；
7. 旧代码存在许可证或来源不清风险。

重写不是失败，而是本轮重构的正常策略。重写前记录旧文件路径与不复用原因，重写后用 smoke test 验证。

---

## 4. 旧架构图处理

旧主模型架构图作废。不得在代码、README、论文草稿、PPT 或图像绘制中参考。最终图像在 `docs/FINAL_MODEL_SPEC.md` 确定后重新绘制，必须与真实代码一致。

---

## 5. 代码规范

1. Python 代码遵循 PEP8；
2. 关键函数与类使用 type hints；
3. 核心类、核心函数必须写 docstring；
4. 关键逻辑必须使用中文注释，术语可保留英文；
5. 张量形状必须注释，如 `[B, T, D]`；
6. 配置集中到 `configs/`，禁止散落硬编码；
7. 路径通过 config 或命令行参数传入；
8. 所有随机性由 `utils/seed.py` 控制；
9. 模型经 `engine/registry.py` 注册；
10. trainer/evaluator 不直接依赖具体模型类。

---

## 6. 指标规范

`utils/metrics.py` 是唯一指标实现。必须输出：

```text
ACC2_Non0
F1_Non0
ACC2_Has0
F1_Has0
MAE
Corr
ACC7
```

所有主模型、候选模块实验、baseline、消融实验都必须用该脚本复算。

---

## 7. 产物保存规范

每个 run：

```text
outputs/<phase>/<dataset>/<model>/<timestamp>_<seed>/
├── config.yaml
├── command.txt
├── train.log
├── metrics_epoch.csv
├── metrics_best.json
├── best_model.pth
├── last_model.pth
├── loss_curve.png
├── predictions_test.csv
└── awaf_weights_test.csv
```

全局登记：

```text
reports/experiment_registry.csv
```

不得覆盖历史 run。

---

## 8. 图像规范

所有用于论文或答辩的图像必须：

1. ≥900 dpi；
2. 标题、坐标轴、图例完整；
3. 字体可读；
4. 文件名规范；
5. 不使用终端截图；
6. 同时保留绘图脚本和源数据。

---

## 9. 表格规范

表格必须详细，输出 xlsx 与 docx。三线表必须包含：模型、数据集、特征版本、seed、均值±标准差、全部指标、是否入论文、日志路径。候选模块选择实验、主对比实验、消融实验分别成表。

---

## 10. README 与结构记录

重构完成后必须更新：

```text
docs/FILE_STRUCTURE.md
README.md
```

内容包括目录树、环境创建、数据准备、训练命令、评估命令、实验产物说明、指标口径说明。


---

## 11. 外部代码与依赖管理规范

如果主模型、候选模块或 baseline 需要外部公开代码，允许 CC/Codex 上网查询 GitHub、论文主页、官方文档并下载，但必须纳入工程管理：

```text
external/                 # 第三方源码原样保存，尽量不改
third_party/              # 经适配的小型第三方模块
models/baselines/         # baseline 适配器
baselines/<model_name>/   # 独立 baseline 工程或子模块
docs/EXTERNAL_SOURCES.md  # 外部来源登记表
env/install_commands.md   # 所有安装命令记录
```

外部代码登记字段：

```text
名称 | URL | commit/release | 许可证 | 下载时间 | 用途 | 是否修改 | 修改文件 | 是否进入论文实验 | 风险备注
```

依赖安装规则：

1. 推荐新建 `E:\Anaconda3\envs\mme_xlstm`；
2. 不同 baseline 依赖冲突时，允许建立 `mme_mlcl`、`mme_casp` 等专属环境；
3. 每次安装后导出 `environment.yml` 和 `pip_freeze.txt`；
4. 不允许在没有记录的情况下安装包；
5. 不允许把未知许可证代码直接混入主模型原创实现。

---

## 12. 文件夹清理与归档规范

重构过程中不得自动删除无用文件夹。应执行“三步法”：

```text
扫描识别 → 生成清理计划 → 用户确认后归档/删除
```

### 12.1 扫描识别

P0、P5、P10 可以扫描以下对象：空目录、失败 run、不完整缓存、重复日志、旧 handoff、异常文件、过时报告、无引用脚本、旧版本临时输出。

### 12.2 清理计划

生成：

```text
reports/cleanup_candidates.md      # 阶段性候选清理清单
reports/final_cleanup_plan.md      # P10 最终清理计划
```

每项必须写明：路径、大小、建议动作、风险、是否可复现、是否已有备份。

### 12.3 执行边界

1. 用户未确认前，不删除、不覆盖、不批量移动；
2. 用户确认后优先归档到 `archives/cleanup_<timestamp>/`；
3. 真正删除只在用户明确说“删除”后执行；
4. 清理后必须检查 README、配置文件、训练入口、评估入口的路径是否仍然有效。



---
## P6M Baseline-Lite 路线 (2026-06-19)
Baseline 路线固定为内部 baseline-lite 轻量复现。外部仓库仅作结构参考/引文来源/原论文报告值来源。
固定 baseline: TFN-lite, LMF-lite, MulT-lite, MISA-lite, SelfMM-lite, MMIM-lite, MLCL-lite, DLF-lite。
第一批: TFN-lite, LMF-lite, MulT-lite, SelfMM-lite。
CASP 不入普通baseline主列(TTA)。DPDF-LQ/DashFusion/R3DG暂不执行。
原论文报告值标注 'Reported by original paper'。Baseline-lite结果必须来自真实训练+utils/metrics.py统一复算。
教学演示/模拟/占位不得进入论文正式实验表。
